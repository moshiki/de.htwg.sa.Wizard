package de.htwg.se.wizard.controller

import de.htwg.se.wizard.controller.EvalStep
import de.htwg.se.wizard.model.ResultTable
import org.scalatest.{Matchers, WordSpec}

class EvalStepSpec extends WordSpec with Matchers {
  "An EvalStep" when {
   /* val resultTable = ResultTable(20, 3, ResultTable.initializeVector(3, 3))
    val roundManager = RoundManager(resultTable = resultTable, currentPlayer = 2)
    val controller = new Controller(roundManager)
    var memento: (RoundManager, ControllerState) = (controller.roundManager.copy(), controller.state)

    "do undoStep" in {

     controller.roundManager = controller.roundManager.copy(currentPlayer = 3)
      controller.undo()
      controller.roundManager.currentPlayer should not be(3)
    }
  }*/

}
